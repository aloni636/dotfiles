-- :me: neo-tree default to float
return {
  window = {
    position = "float", -- "float" for floating window
    mappings = {
      -- ["<esc>"] = "close_window",
      -- ["q"] = "revert_preview",
    },
  },
}
